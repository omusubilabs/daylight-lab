{
  // Build tooling and scripts, kept out of tsconfig.json so node globals stay out of src/.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"]
  },
  "include": ["vite.config.ts", "vitest.config.ts", "scripts"]
}
